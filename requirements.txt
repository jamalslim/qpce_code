# Runtime dependencies for reproducing the manuscript.
#
#     pip install -r requirements.txt
#
# Lower bounds only. For the exact versions the published numbers were produced
# with, use requirements-lock.txt instead.

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3          # train_test_split, which defines the held-out split
matplotlib>=3.7            # figures only, no number depends on it
